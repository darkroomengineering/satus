pre-commit:
  parallel: true
  commands:
    biome:
      glob: '*.{js,ts,jsx,tsx,css,scss,md}'
      run: bun biome check --write "{staged_files}"

post-merge:
  commands:
    update-env:
      run: bunx vercel env pull