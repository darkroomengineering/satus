pre-commit:
  parallel: true
  commands:
    biome:
      glob: "*.{js,ts,jsx,tsx}"
      run: npx biome check --write {staged_files}

post-merge:
  commands:
    update-env:
      run: bunx vercel env pull

