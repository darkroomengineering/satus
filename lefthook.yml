# Lefthook configuration for git hooks
# Cross-platform compatible (Windows, macOS, Linux)
# https://github.com/evilmartians/lefthook

pre-commit:
  parallel: true
  commands:
    biome:
      glob: '*.{js,mjs,ts,jsx,tsx,css,scss}'
      run: bun biome check --max-diagnostics=200 --write --unsafe {staged_files}
      stage_fixed: true
    typecheck:
      glob: '*.{ts,tsx}'
      # Incremental is on: with a single (TS7 native) compiler there is no
      # cross-compiler .tsbuildinfo conflict, and the warm cache is ~4x faster.
      run: bunx tsc --noEmit
      stage_fixed: false

# NOTE: Build checks happen in CI, not pre-push (too slow for dev workflow)
# Uncomment if you want local build verification:
# pre-push:
#   commands:
#     build-check:
#       run: bun run build
#       skip:
#         - merge
#         - rebase

post-merge:
  commands:
    install-deps:
      run: bun i
